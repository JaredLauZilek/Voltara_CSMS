# Public keys only — the anon key is safe in a mobile bundle; the service-role
# key must never appear here (CLAUDE.md §5/§10).
# Local dev from a phone: 127.0.0.1 is the PHONE, so point at the staging
# project, or expose the local stack through a tunnel and use that URL.
EXPO_PUBLIC_SUPABASE_URL=https://lxsfnqshcacsrolmymsg.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
