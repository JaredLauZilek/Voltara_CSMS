// Public barrel — the only file other features / the shell may import from.
export { DocumentsFeature } from './DocumentsFeature';
export { useDocuments } from './hooks';
export type { Document } from './types';
